# ghostty commit pinned for BOTH the GhosttyKit.xcframework binary and the
# vendored runtime resources under Resources/{ghostty,terminfo}.
#
# The xcframework is gitignored (rebuilt per machine); the resources are
# committed. They MUST come from the same ghostty commit — when bumping,
# rebuild ghostty at the new commit and refresh both together, then update
# this file. See CONTRIBUTING.md ("Building GhosttyKit").
#
# Must be the FULL 40-char SHA: release CI fetches this exact object name
# (`git fetch <sha>` rejects an abbreviated prefix).
5659cef41f4f2f7a478d0800a11836fa17e64d66
